import React from 'react';
import MainContainer from '~/components/containers/MainContainer';

const Home = (): React.ReactElement => (
  <MainContainer>
    The Cat API Frontend - HOME PAGE
  </MainContainer>
);

export default Home;
